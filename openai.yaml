interface:
  display_name: "Google Review Builder"
  short_description: "Build reusable, honest Google review flows"
  default_prompt: "Use $google-review-builder to adapt or build a Google Review flow for a business. Ask for the business configuration only when it is missing."
